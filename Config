package.KaenaTransformers = {
    interfaces = (1.0);

    build-system = custom-build;
    build-tools = {
        1.0 = {
            Python3PBuildTool = 2.0;
            Python-wheel = 0.x;
        };
    };

    targets = {
        python = { type = python; };
    };

};
